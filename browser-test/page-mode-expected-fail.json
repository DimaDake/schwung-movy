{
  "note": "The app-loop checks that still fail under schwunggrid=page. Started at 13 (Phase 0); SP-11 took it to 6, SP-17 to 5. All five left are ONE thing, and it is a FIXTURE limit rather than a movy defect: under `page` the page SET is Schwung's, planned from the module's own declared hierarchy, and the module these five use — the suite's `mrdrums` fixture (the one `browser-test/mock-synth.mjs:1210` declares) — declares NO `ui_hierarchy` at all, so the plan is the single fallback page 'Main' while movy's own config gives it four banks (Main/Rand/Global/Preset). Every label below is a check whose subject is one of those banks: the preset-browse block turns the jog three times to reach a Preset page Schwung never plans, so its four assertions fall together, and 'held-step jog switches page' steps off page 0. Said of THAT fixture and not of the suites' modules at large — a fixture that DOES declare a hierarchy, like `hier_params_overflow_two_levels`, plans two pages under `page` and is not what these labels run on. No seam change brings movy's config banks back as Schwung pages; these leave the list when the fixtures stop naming movy banks, or when the jog learns to reach them. SP-17 was the Cause-C half — a filepath dive has no editor in Schwung, so the controller took the click before movy's file browser was reached — and its one label, 'chain page: file-param jog click opens file browser', was DELETED here, on a param Schwung DOES plan. THIS LIST MAY SHRINK AND MUST NEVER GROW. A label here that starts passing must be DELETED (the run fails until it is). A failure not listed here is a regression and fails the run. Owner: docs/schwung-page-migration.md.",
  "labels": [
    "browser opened",
    "browseOrigin captured the pre-open view",
    "Back leaves the file browser",
    "select committed the preset path",
    "held-step jog switches page"
  ]
}
